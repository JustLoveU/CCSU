
server:
  port: 8083

# mysql
spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    #MySQL
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3308/website_university_admin?characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
  mail:
    username: lomtom@qq.com
    password: 改成自己的
    host: smtp.qq.com
    properties:
      mail.smtp.ssl.enable: true
  jackson:
    time-zone: GMT+8
    date-format: yyyy-MM-dd HH:mm:ss
  resources:
    static-locations: classpath:/static/,classpath:/views/

mybatis-plus:
  mapperLocations: classpath:mapper/**/*.xml

logging:
  level:
    com.lomtom: debug
